<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<manialink name="ESC:pb-records.set" id="pb-records.set" version="3">
    <script>
        <!--
        main(){
            declare Integer[] Checkpoints for InputPlayer;
            declare Text TargetString for InputPlayer;
            declare Text TimesHash for InputPlayer;

            Checkpoints = [{$checkpoints->count() > 0 ? $checkpoints->implode(',') : "-1"|noescape}];
            TargetString = "{$targetString|noescape}";
            TimesHash = "{$timesHash|noescape}";
        }
        -->
    </script>
</manialink>