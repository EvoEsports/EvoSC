<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<manialink name="ESC:update-maps-queue" id="update-maps-queue" version="3">
    <script>
        <!--
        main(){
            declare Text QueueHash for LocalUser;
            declare Text[][] QueuedMaps for LocalUser;

            QueueHash = {uniqid()};

            {if $queue != ""}
            QueuedMaps = [{$queue|noescape}];
            {else}
            QueuedMaps = Text[][];
            {/if}
        }
        -->
    </script>
</manialink>