<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<manialink name="ESC:Maps:update-current-map" id="update-current-map" version="3">
    <script>
        <!--
        main(){
            wait(InputPlayer != Null);

            declare Text CurrentMapHash for InputPlayer;
            declare Text[Text] CurrentMap for InputPlayer;

            CurrentMapHash = {uniqid()};
            CurrentMap = ["id" => "{$map->id|noescape}", "author" => {$map->author->NickName}, "name" => {$map->gbx->Name}];
        }
        -->
    </script>
</manialink>