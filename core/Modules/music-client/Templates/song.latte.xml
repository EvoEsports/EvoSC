<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<manialink name="ESC:music-widget.set-song" id="music-widget.set-song" version="3">
    <script>
        <!--
        main(){
            while(InputPlayer == Null){
                sleep(10);
            }

            declare Text[] Music for InputPlayer;

            Music.add("{$song->url|noescape}");
            Music.add({$song->title});
            Music.add({$song->artist});
            Music.add({$song->length});

            log(Music);
        }
        -->
    </script>
</manialink>